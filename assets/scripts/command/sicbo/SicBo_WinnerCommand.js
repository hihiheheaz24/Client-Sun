/*
 * Generated by BeChicken
 * on 11/13/2019
 * version v1.0
 */
(function () {
    var SicBoWinnerCommand;

    SicBoWinnerCommand = (function () {
        function SicBoWinnerCommand() {
        }

        SicBoWinnerCommand.prototype.execute = function (controller) {
            let url = 'api/SicBo/GetBigWinner';
            let subDomainName = cc.SubdomainName.SICBO;
            return cc.ServerConnector.getInstance().sendRequest(subDomainName, url, function (response) {
                var obj = JSON.parse(response);
                return controller.onGetBigWinnerResponse(obj);
            });
        };

        return SicBoWinnerCommand;

    })();

    cc.SicBoWinnerCommand = SicBoWinnerCommand;

}).call(this);